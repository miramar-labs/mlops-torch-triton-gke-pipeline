name: ML Train Test

on:
  push:
    branches: [main]
    paths:
      - ml/train.py
      - ml/test_train.py
      - ml/Dockerfile.train
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: [self-hosted, dgx]

    steps:
      - uses: actions/checkout@v4

      - name: Install test dependencies
        run: pip install -q -r ml/requirements.txt

      - name: Run tests
        run: python -m pytest ml/test_train.py -v

      - name: Build training image
        run: docker build -f ml/Dockerfile.train -t ml-trainer:latest .
