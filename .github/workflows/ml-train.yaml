name: ML Train

on:
  workflow_run:
    workflows: ["ML Train Test"]
    types: [completed]
  workflow_dispatch:
    inputs:
      epochs:
        description: 'Training epochs'
        default: '3'
        required: false
      experiment:
        description: 'MLflow experiment name'
        default: 'text-classifier'
        required: false

permissions:
  contents: read

jobs:
  train:
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    runs-on: [self-hosted, dgx]

    steps:
      - uses: actions/checkout@v5

      - name: Build training image
        run: docker build --no-cache -f ml/Dockerfile.train -t ml-trainer:latest .

      - name: Train model
        run: |
          mkdir -p ml/output
          ONNX_VOLUME="onnx-${{ github.run_id }}"
          docker volume create "$ONNX_VOLUME"
          docker run --rm --gpus all \
            --network host \
            -v "${ONNX_VOLUME}:/output" \
            -v "$HOME/.cache/huggingface:/root/.cache/huggingface" \
            -e MLFLOW_TRACKING_URI="http://localhost:5000" \
            -e MLFLOW_EXPERIMENT_NAME="${{ github.event.inputs.experiment || 'text-classifier' }}" \
            -e EPOCHS="${{ github.event.inputs.epochs || '3' }}" \
            ml-trainer:latest
          docker run --rm -v "${ONNX_VOLUME}:/output" alpine cat /output/model.onnx > ml/output/model.onnx
          docker volume rm "$ONNX_VOLUME"

      - name: Upload ONNX model
        uses: actions/upload-artifact@v4
        with:
          name: onnx-model
          path: ml/output/model.onnx
          retention-days: 30
